package maps;

public class ComputeMain {
	/*
	 * name: compute
	 * syntax:
	 * 		V compute(key, remappingFunction)
	 * 		V compute(K, BiFunction<? super K, ? super V, ? extends V>)
	 * */
	/*
	 * giải thích:
	 * 		key: Khóa cho 1 entry trong map mà bạn muốn tính toán lại giá trị.
	 * 		remappingFunction: Một hàm nhận vào key và giá trị hiện tại (có thể là null), và trả về giá trị mới. 
	 * 		Nếu hàm trả về null, entry sẽ bị xóa.
	 * */
	public static void main(String[] args) {
		// TODO Auto-generated method stub

	}

}
